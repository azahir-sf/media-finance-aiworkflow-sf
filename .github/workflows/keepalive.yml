name: Keepalive

on:
  schedule:
    - cron: "0 6 * * *"   # every day at 6am UTC

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep repo active
        run: echo "keepalive"
